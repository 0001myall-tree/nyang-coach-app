<?xml version="1.0" encoding="utf-8"?>
<!-- 틈새 코칭 카드. 버튼이 없다.
     무엇을 할지 정해주지 않고 한 마디만 건네는 자리라, 실행도 거절도 물을 것이
     없다. 하고 싶은 사람은 냥냥이를 눌러 할 일 목록으로 가고, 아니면 바깥을
     누르거나 그냥 두면 사라진다. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/nudge_gap_scrim"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#66000000"
    android:clickable="true"
    android:focusable="false">

    <ScrollView
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:clipToPadding="false"
        android:fillViewport="false"
        android:scrollbars="none">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/nudge_card_bg"
            android:clickable="true"
            android:orientation="vertical"
            android:paddingStart="20dp"
            android:paddingTop="24dp"
            android:paddingEnd="20dp"
            android:paddingBottom="24dp">

            <TextView
                android:id="@+id/nudge_gap_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:lineSpacingExtra="4dp"
                android:text="지금 잠깐 여유 있나냥?"
                android:textColor="#3D3A4E"
                android:textSize="16sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/nudge_gap_body"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="10dp"
                android:lineSpacingExtra="4dp"
                android:text="이따 할 일이 가벼워지도록 10분만 미리 씨앗을 뿌려볼까냥? 🌱"
                android:textColor="#6B6676"
                android:textSize="15sp" />

            <ImageView
                android:id="@+id/nudge_gap_image"
                android:layout_width="120dp"
                android:layout_height="120dp"
                android:layout_gravity="center_horizontal"
                android:layout_marginTop="16dp"
                android:contentDescription="냥냥코치"
                android:scaleType="fitCenter" />
        </LinearLayout>
    </ScrollView>
</FrameLayout>

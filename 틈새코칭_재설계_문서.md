# [설계 문서] 틈새 코칭 재설계

> 구현 상태: **아직 아무것도 안 만듦.** 2026-09-09에 설계만 확정.
> 현재 코드: `lib/services/gap_coaching_service.dart`, `android/.../GapCoachingCopy.kt`,
> 아이폰은 `lib/services/nyang_banner_nudge.dart`의 `gapNotificationIds`.

---

## 1. 이 기능이 원래 하려던 것

**큰 일을 앞에 두고 있을 때, 지금 이 틈에 끝낼 수 있는 15분짜리 조각 하나를 제안한다.**

글쓰기라면 "15분만 써봐"가 아니라 **"15분 동안 개요만 잡아둘까"**다. 시간은 좁히되 그 안에서 하나가 완결돼야 한다 — 조각을 주되 완성했다는 느낌이 남아야 다음에 또 온다.

멍때리는 시간을 생산적인 시간으로 바꾸는 것이 목적이고, 그 판단 기준은 **오늘 계획의 완료율**이다.

### 지금 못 하고 있는 것

처방이 **키워드 사전에 걸린 일에만** 나간다.

| 이름 | 결과 |
| --- | --- |
| 보고서·리포트·발표·논문 | "개요만 대충 잡아둬도" ✓ |
| 글·원고·에세이·블로그 | "첫문장만 준비해둬도" ✓ |
| 기획·콘텐츠·디자인 | "콘셉트만 생각해둬도" ✓ |
| 운동·청소·장보기 | "뭐부터 준비할지만 정해둬도" ✓ |
| **레퍼런스 정리, 세금계산서, 면접 준비, 이력서 수정, 코드 리뷰, 번역…** | **"10분만 먼저 해두면" — 조각이 없음** ✗ |

사전에 없는 이름은 전부 시간으로만 자른 문구로 떨어진다. 이 기능이 하려던 걸 못 하는 상태다.

**이 앱은 같은 방식으로 이미 여러 번 뚫렸다.** `RoutineDomainCheck` 첫 줄이 그 반성이다 — *"키워드 사전으로 하지 않는다. '방 정리'는 잡혀도 '분리수거'나 '이불 널기'는 놓친다. 사람이 루틴에 적는 이름을 목록으로 따라잡을 수는 없다."* 거기서는 작은 모델 호출로 옮겼는데, 틈새 코칭만 사전이 남아 있다.

---

## 2. 시간대로 두 갈래

하루 최대 2회, 시각은 사용자가 정한다. 그 시각에 **남은 시간이 얼마나 되느냐**로 하는 말이 갈린다.

```
남은 시간이 넉넉하다  → 조각 제안 (일 이름 → 15분 조각)
얼마 안 남았다        → 오늘 건지는 한 수 (앱 판정)
진행 중이다           → 아무 말도 안 함
```

늦은 시각에 "앞 15분만 미리 해두자"는 이상하다. 이따 할 시간이 없는데 미리 해두라는 말이 된다.

**경계**: 취침 시각까지 4시간 이하면 늦은 쪽. 취침 시각을 안 정해둔 사람은 **19시**로 떨어진다.
(`nyang_premium_min_sleep_time` 등 마스터 설정에 취침 시각이 있다.)

19시인 이유는 두 규칙이 어긋나지 않게 하기 위해서다. 취침이 23시면 "4시간 전"이 정확히 19시라, 취침 시각을 적어둔 사람과 안 적어둔 사람이 같은 자리에서 갈린다.

---

## 3. 이른 시각 — 조각 제안

### 규칙

1. 오늘 남은 일 중 하나를 고른다. (지금 규칙 유지: 시작 안 한 것만, 약속은 빼고, 지나간 시각은 "이따"가 아님)
2. 그 이름으로 **15분 안에 완결되는 조각**을 만든다.
3. 소요 시간이 긴 일이 있으면 그것을 먼저 고른다.

### 조각을 만드는 방법

**작은 모델 호출.** 이름을 주고 조각 하나를 받는다. `RoutineDomainCheck`와 같은 방식.

```
'레퍼런스 정리'  → 15분간 자료 목록만 만들어두기
'면접 준비'      → 15분간 예상 질문 세 개만 적어두기
'세금계산서'     → 15분간 필요한 서류만 모아두기
```

사전으로는 이런 게 안 나온다. 네 갈래 중 하나로만 떨어진다.

**실패하면 지금 사전으로 떨어진다.** 통신이 끊겼거나 응답이 이상하면 알림이 안 뜨는 게 아니라 기존 문구가 나간다.

### 15분을 넘기지 않는다

30분은 조각이 아니라 또 하나의 일이다. 15분은 이 앱이 이미 쓰는 단위이기도 하다 — 저항 개입의 `선택권 주기`가 15분짜리 조각으로 들어가 있고, 지금 틈새 문구는 10분이다.

### 크기를 코치가 정하지 않는다

시간으로 자르는 것까지가 앱 몫이고, **그 15분에 정확히 뭘 할지 단계를 앱이 맞히면 안 된다.** 이미 개요를 쓴 사람에게 개요를 권하면 "15분만 써봐"보다 나쁘다. 조각은 **제안**이고, 아니면 사용자가 채팅에서 다시 정한다.

(관련 규칙: 크기를 정하는 쪽과 칭찬하는 쪽이 달라야 한다. 코치가 크기까지 정하면 다 했을 때의 칭찬이 담합이 된다.)

---

## 4. 늦은 시각 — 오늘 건지는 한 수

전부 **폰으로 되는 것**이어야 한다. 틈새 코칭은 사용자가 지금 어디 있는지 모른다. "루틴 지금 해라"는 헬스장에 있어야 하는 사람에게 무리다.

위에서부터 맞는 것 하나.

| 조건 | 제안 |
| --- | --- |
| 진행 중이다 | **안 부름** (하고 있는 사람에게 말 거는 건 방해) |
| 일시정지된 게 있다 | 다시 붙기 — 오늘 가장 가까이 갔던 일이고 문턱이 제일 낮다 |
| 핵심을 안 정했다 | 하나만 짚기 — 핵심은 알림이 붙어 완료율에 직결 |
| 시간을 안 정한 게 있다 | 언제 어디서 할지 정하기 |
| 평소 해내는 양보다 많이 잡았다 | 몇 개 정리하기 |
| 두 주째 못 한 루틴이 있다 | 접어두기 |
| 아무것도 안 적었다 | 추천에서 고르기 |

### 문구 예시

```
집사, '분기 리포트' 12분 하다 멈췄다냥. 5분만 더 붙을까냥?
집사, 핵심 하나만 짚고 갈까냥?
집사, 'sns 글쓰기' 언제 어디서 할지 정해둘까냥?
집사, 오늘 7개는 좀 많다냥. 몇 개 정리할까냥?
집사, 두 주째 못 한 루틴이 있다냥. 잠깐 접어둘까냥?
집사, 어제 미룬 '분기 리포트' 오늘 넣을까냥?
```

### 각 칸의 근거

**일시정지 vs 진행 중은 다르다.** `inProgress`는 타이머가 도는 중 — 이 사람은 틈이 아니다. `isPaused`(`!inProgress && elapsedSeconds > 0`)는 하다가 멈춘 것이고, 얼마나 했는지도 안다. 이쪽이 진짜 틈이다.

**시간이 없으면 잊힌다.** 앱이 이미 아는 사실이다 — *"시각 없이 들어가고, 알림이 안 붙어 그대로 잊힌다"*(`chat_screen.dart`). 할 일 추가 카드에는 "시간 정하기"를 열어뒀는데, 이미 들어간 것들은 아무도 안 챙긴다.

**시간+장소는 기록 탭 조언의 실행 채널이다.** 시작 편차형 조언이 *"언제 어디서 할지까지 같이 정해두면 첫 발이 가벼워져"*인데, 말만 하고 실제로 정하는 자리가 없다. 장소 칸은 없으므로 **메모에 적는다** — 정하는 행위 자체가 효과라 앱이 읽을 필요는 없다.

**"평소보다 많다"의 기준은 해낸 양이다.** 잡은 개수끼리 비교가 아니라, 오늘 잡은 개수 vs 평소 완료 개수. `계획 과다형` 정의와 같다.

**몇 개로 줄일지는 안 정한다.** `"7개는 많다냥. 3개로 줄이자냥"`이 아니라 `"7개는 좀 많다냥. 몇 개 정리할까냥?"`이다. 많다는 것만 알려주고 몇 개로 줄일지는 사용자가 고른다.

크기를 정한 쪽이 나중에 칭찬까지 하면 담합이 되기 때문이다 — 코치가 "3개만 해"라고 정해놓고 "3개 다 했네 잘했어"까지 하면, 다 해도 짜고 친 것처럼 남는다. 크기는 사용자가 정하고 코치는 결과만 본다.

**계획이 없을 때 "하나 적어"는 광활하다.** 빈 종이라 멍때리는 시간이 그대로 이어진다. **추천을 주면 고르는 일**이 된다 — 어제 미룬 것, 요즘 자주 적던 것, 오늘 루틴 중 아직 안 한 것.

### 겹치지 않게 확인한 것

| 이미 있는 것 | 겹침 |
| --- | --- |
| 기록 탭 유형 조언 | **겹치지 않음.** 거기는 주간 회고("너는 이런 사람이야"), 여기는 지금 한 수. 유형 이름을 여기서 대지 않는다 |
| 금요일 루틴 나누기 제안 | **나누기는 거기, 접기는 여기.** 매일 하던 걸 화·목으로 옮기는 것과, 안 하게 된 걸 접는 것은 다르다 |
| 낮 12시 알림 | **거기는 아직 아무것도 안 한 사람에게만.** 여기는 지금 남은 하루를 본다 |
| 채팅 저항 개입(쪼개기) | **거기는 사용자가 "하기 싫어" 했을 때.** 여기는 요청 없이 먼저 |

---

## 5. 판단을 Dart로 모은다

### 지금

안드로이드는 알림이 뜨는 순간에 **코틀린이 직접** 문구를 만든다(`GapCoachingCopy.kt`). 앱이 꺼져 있어도 돌아야 하기 때문이다. 그래서 **키워드 목록이 코틀린에 한 벌, Dart에 한 벌** 있다. 한쪽만 고치면 두 폰이 다른 말을 한다.

위 규칙을 다 넣으면 그것도 두 벌이 된다.

### 바꾸는 것

```
지금:   코틀린이 [목록 읽고 → 판단하고 → 문장 만들고 → 띄움]
바꾼 뒤: Dart가   [판단하고 → 문장 만들어 저장]
        코틀린이 [읽어서 → 유효한지만 확인 → 띄움]
```

**낡는 문제는 할 일 id로 막는다.** 문장과 함께 그 할 일 id를 저장하고, 코틀린은 띄우기 전에 **그 일이 아직 안 끝났는지만** 본다. 끝났으면 기본 문구로 떨어진다. 목록 하나만 보면 되므로 판단이 두 벌이 되지 않는다.

### 왜 미리 만들 수밖에 없나

**아이폰은 알림을 예약할 때 문구가 굳는다.** 그 시각이 되면 iOS가 저장된 문구를 띄우는 것이라 앱 코드가 낄 자리가 없다.

특정 시각에 아이폰에서 코드를 돌리려면 서버가 조용한 푸시로 깨워야 하는데, **이 앱에는 푸시 기반이 아예 없다**(`firebase_messaging` 미설치). 넣는 것부터가 큰 작업이고 iOS는 조용한 푸시 배달을 보장하지도 않는다.

안드로이드만 발동 시점에 만들면 두 폰이 또 갈린다. 그래서 미리 만드는 쪽으로 맞춘다.

### 나중에 크게 갈 때

서버가 그 시각에 만들어 푸시로 쏘는 것이 정답이다. 할 일 데이터가 이미 Firestore에 있어 서버가 읽을 수 있고, 낡을 일이 없고, 양쪽 폰이 똑같이 동작한다. 다만 푸시 인프라가 새로 필요하다.

---

## 6. 비용

**하루 1회, 마스터 사용자 한 명당 월 8원.**

```
입력  지시문 + 할 일 이름 몇 개  ≈ 250 토큰
출력  조각 한 줄               ≈  50 토큰
                             ─────────
                               300 토큰 × 30일 = 9,000 토큰

9,000 × (868원 / 1,000,000) ≈ 8원
```

(gpt-5-mini 혼합 단가 100만 토큰당 868원 — `api_usage_limit_service.dart`)

### 반드시 있어야 하는 잠금

`sync()`는 **앱을 열 때마다 돈다.** 거기서 그냥 부르면 하루에 앱 연 횟수만큼 결제된다. 하루 30번 열면 8원이 240원이 된다.

낮 12시 알림에서 같은 자리를 이미 겪었다(거기는 울린 시점에 도장을 찍어 막았다). **문구는 그날 처음 열 때 한 번만 만들고, 예약을 다시 거는 것은 열 때마다 해도 공짜다.**

---

## 7. 오래 보이게

목적이 "폰 봤을 때 우연히 볼 수 있게"이므로 지금 설정이 짧다.

| | 지금 | 바꿀 것 |
| --- | --- | --- |
| 안드로이드 오버레이 | 5분 | **15분** (같은 층의 다른 알림과 같게) |
| 아이폰 배너 | 일반 | **`timeSensitive`** — 방해금지 모드를 뚫는다. 지속 시간은 iOS가 정하므로 앱이 못 바꾼다 |
| 알림 전반 | 한 줄에서 잘림 | **`BigTextStyle`** — 펼치면 전문이 보인다 |

5분으로 잡았던 이유는 *"'지금 잠깐 여유 있나'라고 묻는 말이라 15분 뒤엔 뜻이 흐려진다"*였다. 새 목적에는 안 맞는다 — `개요 15분만 잡아둘까?`는 30분 뒤에 봐도 유효하다.

### 글자 수

**28자는 잰 값이 아니라 어림값이다.** 실제로 잘리는 것은 폭이라 기기·글꼴 크기마다 다르다.

- **안드로이드 오버레이 카드**: `maxLines` 제한이 없다. **안 잘린다.** (지금 기본 문구도 30자다)
- **알림**: 접힌 상태는 한 줄. 넘치는 부분은 **못 볼 수도 있다** → 중요한 말을 앞에

---

## 8. 만드는 순서

1. **판단을 Dart로 모으기** — 네이티브 사전 중복 제거. 이게 먼저여야 아래가 두 벌이 안 된다
2. **시간대로 갈라 늦은 쪽 메뉴 얹기** — 여기까지 API 0원
3. **오래 보이게** (오버레이 15분 / `timeSensitive` / `BigTextStyle`)
4. **이른 쪽 조각 만들기를 API로** — 하루 1회 잠금과 실패 폴백을 같이

---

## 9. 이 문서에서 안 다룬 것

- **낮 12시 플래너 알림**은 별개다. 2026-09-09에 이미 손봤다(커밋 `bda128d`) — 유형 짚기·칭찬 주 1회, 며칠째 안 오는 사람 사다리
- **장소 칸을 만들지 여부.** 지금은 메모로 간다. 나중에 "그 장소에 있을 때 알려주기" 같은 게 생기면 그때 칸을 판다
